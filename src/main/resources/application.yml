spring:
  application:
    name: ai-automation-service

  jmx:
    enabled: false

  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB

  datasource:
    url: jdbc:postgresql://dpg-d8470ofaqgkc73adpk6g-a.oregon-postgres.render.com:5432/radha_ai
    username: radha_ai_user
    password: vKfXPbeFasUD1PwLSmwQDAPTpka0kTX1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false

  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o
          max-tokens: 4096
          temperature: 0.7
      embedding:
        options:
          model: text-embedding-3-small
    ollama:
      chat:
        enabled: false
      embedding:
        enabled: false
    vectorstore:
      qdrant:
        host: ${QDRANT_HOST}
        port: ${QDRANT_PORT}
        api-key: ${QDRANT_API_KEY}
        collection-name: askoxycollection
        initialize-schema: true
        use-tls: true
    anthropic:
      api-key: DISABLED

     # added — forces TLS on Railway
    connectiontimeout: 5000
    timeout: 5000
    writetimeout: 5000

  mail:
    host: smtp.gmail.com
    port: ${SPRING_MAIL_PORT}
    username: venagantirishi@gmail.com
    password: bptd hnds uwxu smmv

    properties:
      mail:
        debug: true

        smtp:
          auth: true

          starttls:
            enable: true
            required: true

          connectiontimeout: 30000
          timeout: 30000
          writetimeout: 30000

app:
  gmail:
    imap:
      host: imap.gmail.com
      port: 993
      protocol: imaps
      username: venagantirishi@gmail.com
      password: bptd hnds uwxu smmv
      inbox-folder: INBOX

    poll-interval-ms: 60000
    lookback-hours: 24
    campaign-validity-days: 7

  email:
    from-name: AskOxy Team

  # ─── Multi-Sender Company Accounts (Round-Robin Bulk Campaigns) ──────────────
  # GoDaddy SMTP: smtpout.secureserver.net : 587
  senders:
    - email: sales@oxyglobaltech.xyz
      password: Test@12345
      name: OxyGlobal Sales

    - email: marketing@oxyglobaltech.info
      password: Test@1235
      name: OxyGlobal Marketing

    - email: CEO@oxyglobaltech.net
      password: Test*@1235
      name: OxyGlobal CEO

    - email: VThatavarti@gccmate.com
      password: Gccmate@1919
      name: GCC Mate

    - email: VThavarti@oxygold.ai
      password: Oxygold@1919
      name: OxyGold AI

ultramsg:
  instance-id: instance176023
  api-url: https://api.ultramsg.com/instance176023/messages/chat
  token: ${ULTRAMSG_TOKEN}
  admin-number: 919849257032

useronboard:
  mail:
    host: ${spring.mail.host}
    port: ${spring.mail.port}
    username: ${spring.mail.username}
    password: ${spring.mail.password}
  qdrant:
    host: ${spring.ai.vectorstore.qdrant.host}
    port: ${spring.ai.vectorstore.qdrant.port}
    api-key: ${spring.ai.vectorstore.qdrant.api-key}
    collection-name: ${spring.ai.vectorstore.qdrant.collection-name}
    use-tls: ${spring.ai.vectorstore.qdrant.use-tls}



radhaclone:
  mail:
    host: smtp.gmail.com
    port: 587
    username: siddushiva2699@gmail.com
    password: upzw endy nhuu htag
  qdrant:
    host: ${QDRANT_HOST}
    port: ${QDRANT_PORT}
    api-key: ${QDRANT_API_KEY}
    collection-name: askoxy_live
    use-tls: true

server:
  forward-headers-strategy: framework

radha:
  email:
    from: siddushiva2699@gmail.com
    to: bsudheesh123@gmail.com
  n8n:
    enabled: true
    webhook-base-url: https://siddushiva.app.n8n.cloud/webhook

gemini:
  api:
    key: AIzaSyCtOA1KiqxAGhb71srDoLz5KBp97fHuTeI

management:
  health:
    redis:
      enabled: false

logging:
  level:
    root: INFO

    com.askoxy.emailautomation.service.GmailPollingService: DEBUG
    com.askoxy.emailautomation.service.ClientReplyProcessorService: DEBUG

    org.springframework.scheduling: INFO

    org.springframework.mail: DEBUG
    org.eclipse.angus.mail: DEBUG

  file:
    name: logs/mail-automation.log

aws:
  s3:
    bucket: radha-clone
    region: ap-south-1
    access-key: ${AWS_ACCESS_KEY_ID:}
    secret-key: ${AWS_SECRET_ACCESS_KEY:}
    presigned-url-expiry-days: 7